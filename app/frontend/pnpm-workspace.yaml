# Single-package "workspace" — pnpm ≥ 11 requires `packages` to exist as
# soon as this file does. Luna is NOT a monorepo.
packages:
  - '.'

# Whitelist the two native-binary post-install scripts we already trust.
onlyBuiltDependencies:
  - '@swc/core'
  - esbuild
